//! Version-pin stub: this package exists solely so `cargo metadata` resolves
//! the pinned ziskos checkout (via Cargo.lock) for the C-direct guest link.
//! No Rust is built into the guest; see build.sh.
#![no_std]
