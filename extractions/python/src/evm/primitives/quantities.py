# Generated by Sail's Python backend. Do not edit by hand.
# Sail source: primitives/quantities.sail
from __future__ import annotations

from .._runtime import (
    BitWidth,
    Bits,
    SailError,
    SailExit,
    U16,
    U32,
    U64,
    U8,
    Uint,
    UintEnum,
    Unsigned,
)
from dataclasses import dataclass
from typing import Annotated, TypeAlias
from pydantic import ConfigDict, model_validator
from pydantic.dataclasses import dataclass as pydantic_dataclass
from typing_extensions import Self
from evm.prelude import (
    word,
    u256,
)

account_nonce: TypeAlias = Uint

block_number: TypeAlias = Uint

block_timestamp: TypeAlias = Uint

blob_schedule_target_count: TypeAlias = int

blob_schedule_max_count: TypeAlias = int

transaction_blob_limit: TypeAlias = int

transaction_blob_count: TypeAlias = Uint

blob_gas_used: TypeAlias = int

transaction_blob_gas: TypeAlias = int

class excess_blob_gas(Unsigned):
    LOWER = 0
    UPPER = 2994028288

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class blob_fee_update_fraction(Unsigned):
    LOWER = 1
    UPPER = 11684671

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

blob_schedule_fee_update_fraction: TypeAlias = int

protocol_deployed_code_size_limit: TypeAlias = int

protocol_initcode_size_limit: TypeAlias = int

protocol_transaction_total_gas_limit: TypeAlias = int

protocol_transaction_regular_gas_limit: TypeAlias = int

chain_identifier: TypeAlias = Uint

slot_number: TypeAlias = Uint

withdrawal_index: TypeAlias = Uint

validator_index: TypeAlias = Uint

withdrawal_amount: TypeAlias = Uint

ssz_uint: TypeAlias = U64

ssz_offset: TypeAlias = U32

class ssz_offset_index(Unsigned):
    LOWER = 0
    UPPER = 1073741823

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class frame_depth(Unsigned):
    LOWER = 0
    UPPER = 1024

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class push_width(Unsigned):
    LOWER = 0
    UPPER = 32

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class operand_stack_height(Unsigned):
    LOWER = 0
    UPPER = 1024

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class stack_index(Unsigned):
    LOWER = 0
    UPPER = 1023

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

@dataclass(slots=True)
class StackPointer:
    storage: Annotated[Bits, BitWidth(64)]
    height: operand_stack_height

class stack_slot_count(Unsigned):
    LOWER = 0
    UPPER = 1024

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class stack_operation_index(Unsigned):
    LOWER = 1
    UPPER = 16

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class deep_stack_index(Unsigned):
    LOWER = 17
    UPPER = 235

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class log_topic_count(Unsigned):
    LOWER = 0
    UPPER = 4

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class y_parity(Unsigned):
    LOWER = 0
    UPPER = 1

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class merkle_depth(Unsigned):
    LOWER = 0
    UPPER = 64

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

opcode: TypeAlias = U8

class PrecompileId(UintEnum):
    NotPrecompile = Uint(0)
    Ecrecover = Uint(1)
    Sha256 = Uint(2)
    Ripemd160 = Uint(3)
    Identity = Uint(4)
    Modexp = Uint(5)
    Bn254Add = Uint(6)
    Bn254Mul = Uint(7)
    Bn254Pairing = Uint(8)
    Blake2f = Uint(9)
    KzgPointEvaluation = Uint(10)
    BlsG1Add = Uint(11)
    BlsG1Msm = Uint(12)
    BlsG2Add = Uint(13)
    BlsG2Msm = Uint(14)
    BlsPairing = Uint(15)
    BlsMapFpToG1 = Uint(16)
    BlsMapFp2ToG2 = Uint(17)
    P256Verify = Uint(18)

precompile_id: TypeAlias = PrecompileId

blake2_rounds: TypeAlias = U32

bls_discount: TypeAlias = U16

class bloom_bit_index(Unsigned):
    LOWER = 0
    UPPER = 2047

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class bloom_u64_index(Unsigned):
    LOWER = 0
    UPPER = 31

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class bloom_u64_bit(Unsigned):
    LOWER = 0
    UPPER = 63

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class ancestor_hash_count(Unsigned):
    LOWER = 0
    UPPER = 256

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

ancestor_index: TypeAlias = U8

def word_of_account_nonce(value: account_nonce) -> account_nonce:
    return account_nonce(value)

def word_of_withdrawal_amount(value: withdrawal_amount) -> withdrawal_amount:
    return withdrawal_amount(value)

def word_of_slot_number(value: withdrawal_amount) -> withdrawal_amount:
    return withdrawal_amount(value)

def word_of_block_number(value: withdrawal_amount) -> withdrawal_amount:
    return withdrawal_amount(value)

def word_of_block_timestamp(value: withdrawal_amount) -> withdrawal_amount:
    return withdrawal_amount(value)

def word_of_chain_identifier(value: chain_identifier) -> chain_identifier:
    return chain_identifier(value)

host_access: TypeAlias = Uint

source_pointer: TypeAlias = Uint

source_length: TypeAlias = Uint

stateless_input_pointer: TypeAlias = Uint

stateless_input_length: TypeAlias = Uint

scratch_pointer: TypeAlias = Uint

scratch_length: TypeAlias = Uint

log_data_pointer: TypeAlias = Uint

log_data_length: TypeAlias = Uint

output_pointer: TypeAlias = Uint

output_length: TypeAlias = Uint

calldata_pointer: TypeAlias = Uint

calldata_length: TypeAlias = Uint

class word_byte_count(Unsigned):
    LOWER = 0
    UPPER = 32

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

journal_cursor: TypeAlias = Uint

storage_generation: TypeAlias = Uint

memory_base: TypeAlias = Uint

memory_length: TypeAlias = Uint

memory_height: TypeAlias = memory_length

@pydantic_dataclass(config=ConfigDict(strict=True, arbitrary_types_allowed=True), frozen=True, slots=True, kw_only=True)
class MemoryRangeFieldsValidity:
    off: int
    len: int

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not (((0 <= self.off) and ((0 <= self.len) and ((self.off + self.len) <= ((2 ** 32) - 1))))):
            raise ValueError("MemoryRangeFieldsValidity violates Sail constraint memory_valid_range('off, 'len)")
        return self

@pydantic_dataclass(config=ConfigDict(strict=True, validate_assignment=True, revalidate_instances="always", arbitrary_types_allowed=True), slots=True, kw_only=True)
class MemoryRangeFields:
    validity: MemoryRangeFieldsValidity
    off: int
    len: int

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not ((int(self.off) == self.validity.off)):
            raise ValueError("MemoryRangeFields.off violates Sail type int('off)")
        if not ((int(self.len) == self.validity.len)):
            raise ValueError("MemoryRangeFields.len violates Sail type int('len)")
        return self

MemoryRange: TypeAlias = MemoryRangeFields

@pydantic_dataclass(config=ConfigDict(strict=True, arbitrary_types_allowed=True), frozen=True, slots=True, kw_only=True)
class MemoryAccessFieldsValidity:
    off: int
    len: int
    required: int

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not ((((0 <= self.off) and ((0 <= self.len) and ((self.off + self.len) <= ((2 ** 32) - 1)))) and ((0 <= self.required) and (((self.len == 0) and ((self.off == 0) and (self.required == 0))) or ((0 < self.len) and (self.required == (self.off + self.len))))))):
            raise ValueError("MemoryAccessFieldsValidity violates Sail constraint memory_access_relation('off, 'len, 'required)")
        return self

@pydantic_dataclass(config=ConfigDict(strict=True, validate_assignment=True, revalidate_instances="always", arbitrary_types_allowed=True), slots=True, kw_only=True)
class MemoryAccessFields:
    validity: MemoryAccessFieldsValidity
    range: MemoryRangeFields
    requested_height: int

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not ((int(self.requested_height) == self.validity.required)):
            raise ValueError("MemoryAccessFields.requested_height violates Sail type int('required)")
        return self

MemoryAccess: TypeAlias = MemoryAccessFields

def memory_range(off: int, sail_len: int) -> MemoryRangeFields:
    return MemoryRangeFields(validity=MemoryRangeFieldsValidity(off=int(off), len=int(sail_len)), off=int(off), len=int(sail_len))

code_pointer: TypeAlias = Uint

code_length: TypeAlias = Uint

class code_scan_position(Unsigned):
    LOWER = 0
    UPPER = 4294967263

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

def word_of_nat_byte_count(value: int) -> word:
    if (int(value) < int((1 << 256))):
        return word(u256(value))
    else:
        if not (False):
            raise SailError("sail/primitives/quantities.sail:615.20-615.21")
        raise SailExit(None)

def word_of_source_byte_count(value: int) -> word:
    return word(word_of_nat_byte_count(value))

EMPTY_MEMORY_RANGE: MemoryRangeFields = memory_range(0, 0)

EMPTY_MEMORY_ACCESS: MemoryAccessFields = MemoryAccessFields(validity=MemoryAccessFieldsValidity(off=0, len=0, required=0), range=EMPTY_MEMORY_RANGE, requested_height=0)
