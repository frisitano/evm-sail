# Generated by Sail's Python backend. Do not edit by hand.
# Sail source: primitives/ssz.sail
from __future__ import annotations

from .._runtime import (
    Uint,
    Unsigned,
)
from typing import TypeAlias
from pydantic import ConfigDict, model_validator
from pydantic.dataclasses import dataclass as pydantic_dataclass
from typing_extensions import Self
from evm.primitives.quantities import (
    source_length,
    source_pointer,
)
from evm.primitives.bytes import StatelessInputSlice

class transaction_count(Unsigned):
    LOWER = 0
    UPPER = 1048576

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class transaction_index(Unsigned):
    LOWER = 0
    UPPER = 1048575

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

class block_access_index(Unsigned):
    LOWER = 0
    UPPER = 1048577

    def _in_range(self, value: int) -> bool:
        return self.LOWER <= value <= self.UPPER

@pydantic_dataclass(config=ConfigDict(strict=True, arbitrary_types_allowed=True), frozen=True, slots=True, kw_only=True)
class BoundedSszListRefValidity:
    maximum: int

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not (((0 <= self.maximum) and (self.maximum <= ((2 ** 32) - 1)))):
            raise ValueError("BoundedSszListRefValidity violates Sail constraint source_valid_length('maximum)")
        return self

@pydantic_dataclass(config=ConfigDict(strict=True, validate_assignment=True, revalidate_instances="always", arbitrary_types_allowed=True), slots=True, kw_only=True)
class BoundedSszListRef:
    validity: BoundedSszListRefValidity
    bytes: StatelessInputSlice
    count: Uint
    max_item_length: source_length

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not ((0 <= int(self.count) <= self.validity.maximum)):
            raise ValueError("BoundedSszListRef.count violates Sail type range(0, 'maximum)")
        return self

@pydantic_dataclass(config=ConfigDict(strict=True, arbitrary_types_allowed=True), frozen=True, slots=True, kw_only=True)
class BoundedSszListCursorValidity:
    maximum: int

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not (((0 <= self.maximum) and (self.maximum <= ((2 ** 32) - 1)))):
            raise ValueError("BoundedSszListCursorValidity violates Sail constraint source_valid_length('maximum)")
        return self

@pydantic_dataclass(config=ConfigDict(strict=True, validate_assignment=True, revalidate_instances="always", arbitrary_types_allowed=True), slots=True, kw_only=True)
class BoundedSszListCursor:
    validity: BoundedSszListCursorValidity
    items: BoundedSszListRef
    index: Uint
    current: source_pointer

    @model_validator(mode="after")
    def validate(self) -> Self:
        if not ((0 <= int(self.index) <= self.validity.maximum)):
            raise ValueError("BoundedSszListCursor.index violates Sail type range(0, 'maximum)")
        return self

TransactionListRef: TypeAlias = BoundedSszListRef

TransactionListCursor: TypeAlias = BoundedSszListCursor

WithdrawalListRef: TypeAlias = BoundedSszListRef

WithdrawalListCursor: TypeAlias = BoundedSszListCursor

WitnessNodeListRef: TypeAlias = BoundedSszListRef

WitnessNodeListCursor: TypeAlias = BoundedSszListCursor

WitnessCodeListRef: TypeAlias = BoundedSszListRef

WitnessCodeListCursor: TypeAlias = BoundedSszListCursor

WitnessHeaderListRef: TypeAlias = BoundedSszListRef

WitnessHeaderListCursor: TypeAlias = BoundedSszListCursor
