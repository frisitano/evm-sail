# Generated by Sail's Python backend. Do not edit by hand.
# Sail source: _sail/arith.sail
from __future__ import annotations

from .._runtime import (
    sail_tdiv_int,
)

def _shl_int_general(m: int, n: int) -> int:
    if (int(n) >= 0):
        return (int(m) << int(n))
    else:
        return (int(m) >> int((-int(n))))

def _shr_int_general(m: int, n: int) -> int:
    if (int(n) >= 0):
        return (int(m) >> int(n))
    else:
        return (int(m) << int((-int(n))))

def fdiv_int(n: int, m: int) -> int:
    if (((int(n) < 0)) & ((int(m) > 0))):
        return (int(sail_tdiv_int((int(n) + 1), m)) - 1)
    else:
        if (((int(n) > 0)) & ((int(m) < 0))):
            return (int(sail_tdiv_int((int(n) - 1), m)) - 1)
        else:
            return sail_tdiv_int(n, m)

def fmod_int(n: int, m: int) -> int:
    return (int(n) - int((int(m) * int(fdiv_int(n, m)))))
