# RLP cursor proofs

This directory contains stable, hand-written proofs over the generated Coq
model. Unlike `../model/`, it is not regenerated by Sail.

`RlpCursor.v` establishes the proof boundary used by the executable RLP API:
decoding returns an item whose source length is the consumed width, and the
caller advances its cursor explicitly by that width.
