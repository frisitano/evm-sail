# Coq Extraction

`make extract-coq` at the repository root generates the contract relations and
complete model under `contracts/` and `model/` in this directory. The generated
Coq sources are kept in version control.
