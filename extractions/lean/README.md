# Lean Extraction

`make extract-lean` at the repository root generates and compiles the complete
Lean 4 model under `evm/` in this directory. The generated Lean sources are
kept in version control; only `evm/.lake/build/` is ignored.
